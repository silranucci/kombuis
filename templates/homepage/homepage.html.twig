{% extends 'base.html.twig' %}

{% block main %}
    <div class="row m-5">
        <div class="col-6">
            <div class="card">
                <div class="card-header">
                    Expiring Product
                </div>
                <div class="card-body">
                    {{ include('homepage/_expiring_product.html.twig') }}
                    <a href="{{ path('app_expiring_product') }}" class="btn btn-primary">Show All</a>
                </div>
            </div>
        </div>

        <div class="col-6">
            <div class="card">
                <div class="card-header">
                    Product Under Safety Stock
                </div>
                <div class="card-body">
                    {{ include('homepage/_product_under_safety_stock.html.twig') }}
                    <!-- <a href="{{ path('app_expiring_product') }}" class="btn btn-primary">Show All</a> -->
                </div>
            </div>
        </div>
    </div>

{% endblock %}